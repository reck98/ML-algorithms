# Streamlit Frontend

This folder contains a Streamlit frontend for the car price prediction FastAPI backend.

## Run

From project root:

```bash
pip install -r frontend/requirements.txt
streamlit run frontend/app.py
```

## Backend

Default backend URL in the UI is:

`http://localhost:8012`

You can change it from the sidebar.
